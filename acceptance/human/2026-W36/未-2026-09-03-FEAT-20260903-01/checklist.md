# 人工验收清单：FEAT-20260903-01

- 任务编号：FEAT-20260903-01
- 人工验收绑定：acceptance/human/2026-W36/2026-09-03-FEAT-20260903-01/binding.md
- 验收合同：agents-results/2026-09-03/research-preview-access/acceptance-contract.md
- 合同版本：1
- 清单状态：草稿
- 所需人工角色：项目负责人或数学研究者代表
- 清单负责人：项目负责人
- 批准证据：本轮仅建立草稿，尚未批准
- 执行结果：acceptance/human/2026-W36/2026-09-03-FEAT-20260903-01/runs/<run-id>/result.md

本清单只判断目标研究者是否能理解并完成研究预览准入闭环，不重复自动化已覆盖的错误码、Cookie 属性、持久化、哈希、API 权限或截图断言。当前为未执行草稿；机器 `PASS` 不等于人工签署。

## H-01

- 验收问题：目标研究者能否在没有隐藏实现说明的情况下，理解邀请码、申请和访客模式的差异，并完成获邀进入、刷新恢复和退出？
- 必须人工判断的原因：文案是否易懂、边界是否值得信任、流程是否符合研究者心智模型，无法只由选择器、接口和像素断言决定。
- 闭环名称：数学研究者从公开入口选择合适的准入方式，使用获批邮箱与邀请码进入工作区，刷新后继续工作并安全退出。
- 验收角色：项目负责人或被明确授权的数学研究者代表，使用本次本地验收邮箱和一次性邀请码。
- 进入方式：在受支持浏览器中打开 `http://127.0.0.1:4173/`；从公开准入层正常操作，不使用内部 JSON、调试路由、开发者工具注入状态或直接调用内部函数。
- 要做的事：先理解“申请研究预览”、“使用邀请码”和“以访客身份浏览”三条路径；然后使用指定邮箱和邀请码进入，刷新页面，确认仍在同一会话，最后退出并确认回到准入层。
- 前置条件：本次候选源码的域、HTTP 集成、工作区授权、前端和浏览器门禁全部通过；真实本地服务已启动；验收邮箱和单次邀请码由启动端一次性提供，不写入仓库。
- 验收步骤：
  1. 打开本地入口，不阅读实现说明，判断三条路径的作用与边界是否清楚。
  2. 打开申请表单，检查机构、角色、研究方向和用途是否易于理解；可提交一次测试申请，确认页面只告知待审核，不冒充已开通。
  3. 返回邀请码入口，先输入一个错误码，确认错误提示可理解且不泄露账户状态；再使用本次验收邮箱和一次性邀请码进入。
  4. 在控制台中通过正常导航打开两个代表视图，然后刷新页面，确认会话恢复不会把人送回假登录态。
  5. 打开账户视图，确认只显示当前邮箱和授权范围，不重新显示邀请码；点击退出，确认真实工作区内容从页面消失并回到准入层。
  6. 重新使用同一邀请码，确认不能再次进入；切换到访客模式，判断页面是否清楚说明当前仅为演示数据。
- 预期观察：三条路径的权限和后果可理解；申请只进入待审核；错误状态不泄露邮箱或邀请详情；兑换、刷新恢复和退出是一条连续闭环；退出后不残留真实数据；重放被拒绝；访客页始终表达为演示。
- 副作用确认：提交申请会在本地运行目录中新增一条待审核记录；邀请码首次兑换后永久标记为已使用；退出会撤销会话。这些是本地验收数据，不是生产账户或发布证据。
- 失败表现：无法连接服务时显示可重试错误；错码或重放时留在准入层；会话恢复失败时不渲染真实工作区；退出失败时不假装成已退出。任一结果不诚实或无法恢复时记录具体步骤并停止签署。
- 判断标准：只有目标研究者可独立说明三条路径的区别，并完成兑换、恢复、退出和重放拒绝闭环，全程没有真实数据残留、误导文案或无法恢复的阻塞时，验收人才可在新 run 中签署 `PASS`。
- 预计时长：10 分钟
- 是否阻塞发布：是
- 结果记录规则：将签署后的观察和结论写入新的 `acceptance/human/2026-W36/2026-09-03-FEAT-20260903-01/runs/<run-id>/result.md`；不得通过修改本草稿记录执行结果。逻辑路径不写 `未-`，物理目录由状态同步命令维护。
