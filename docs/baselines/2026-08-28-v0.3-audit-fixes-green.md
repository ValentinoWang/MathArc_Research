# MathArc Research v0.3 audit-fix baseline - GREEN LOCAL REPLAY

Date: 2026-08-28 (Asia/Shanghai)

## Status

**GREEN LOCAL REPLAY / REPOSITORY RELEASE BOUNDARY**

This baseline validates source commit `d542494b23090fdc80111c8179322d8c53e56f3c`. It supersedes the pending replay record for the same v0.3 slice. It does not claim that partial or not-started milestones in `V03_IMPLEMENTATION_STATUS.md` are complete.

## Authoritative command

```bash
cd Projects/MathArc_Research
uv venv --python 3.12 .venv
uv pip install --python .venv/bin/python -e '.[research,dev]'
PATH="$PWD/.venv/bin:$PATH" make ci
```

Result: exit 0.

## Results

- Source commit: `d542494b23090fdc80111c8179322d8c53e56f3c`
- Unit and regression tests: 175 passed, 10 explicitly skipped
- Strict typing: 43 `matharc/v02` source files, 0 errors
- Architecture preflight: 6 passed
- v0.1 engineering acceptance: PASS
- v0.2 research acceptance: 14/14 gates PASS
- Deterministic v0.1 and v0.2 demos: generated and validated
- Codex integration: available, proposal-only, no acceptance authority
- Frankl q=6 exactly-two Python cold replay: byte-equivalent JSON check PASS

The v0.2 namespace remains at `0.2.0` by compatibility contract. The repository-level release identifier is the annotated Git tag `v0.3`.

## Environment

- macOS 26.0.1, arm64
- Python 3.12.11
- mypy 2.3.1
- `matharc-research` installed editable with `[research,dev]`

## Remote boundary

GitHub Actions runs for the branch ended before runner assignment with empty step lists. They are not code-failure evidence and are not claimed as passing evidence. This local replay is the release gate for the v0.3 repository tag.
