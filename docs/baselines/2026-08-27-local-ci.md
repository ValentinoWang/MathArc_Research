# MathArc Research 本地 CI 基线 — 2026-08-27

> 这是 Gate 0 / G0-c 的首份版本化基线工件。它记录的是审计时 `main@59e19648aae31eba56ee38168c8ca5628ecf1dc0` 的本地结果，不代表后续提交自动继承绿色状态；每个大里程碑都必须重新生成新的基线记录。

## 基线身份

- Repository: `ValentinoWang/Harness_Engineering`
- Project: `Projects/MathArc_Research`
- Commit: `59e19648aae31eba56ee38168c8ca5628ecf1dc0`
- Authority mode: GitHub Actions runner/额度受限期间，以项目 venv 下本地 `make ci` 为权威门
- Command:

```bash
cd Projects/MathArc_Research
PATH="$PWD/.venv/bin:$PATH" make ci
```

## 审计记录结果

- Unit tests: `Ran 155 tests ... OK (skipped=2)`
- Type check: `mypy --strict`，40 个 v0.2 源文件，0 错误
- v0.1 acceptance: PASS
- v0.2 acceptance: PASS
- Frankl q=6 cold replay: PASS，输出与冻结证书逐字节一致
- Overall exit code: `0`

## 环境边界

本基线是在安装了项目 venv（含 z3）的环境中取得。审计同时发现 `examples/serve_workspace_v02.py` 在未安装 `matharc` 的系统解释器下存在源码检出 import 耦合；该问题在后续 v0.3 修复提交中单独处理，因此不能用本文件反向宣称后续提交已经通过同一基线。

## 复现与更新纪律

1. 大里程碑前后都执行完整 `make ci`，不要只运行新增测试。
2. 新基线文件必须记录 commit SHA、命令、测试数量、mypy 文件数量、验收结果与环境差异。
3. 若任一项未执行，写 `NOT RUN`，不得用历史绿色结果代替。
4. GitHub Actions 恢复并真实执行所有 steps 后，本地基线与远程 required checks 并存；在 runner 未实际执行前不得写“远程 CI 全绿”。
