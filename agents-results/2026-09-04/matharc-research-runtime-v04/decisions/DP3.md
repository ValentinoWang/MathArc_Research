# DP3 — 首版试点部署目标

- 日期：2026-09-04
- 确认人：用户（本次 v2 修订请求）
- 决定：首版试点部署目标为 Linux + systemd。运行数据、运行日志和备份使用明确的持久目录；密钥只从外部 secret source 注入；反向代理和 TLS 负责外部访问边界。
- 证据边界：本决定只锁定部署目标与安全边界，不证明任何生产主机、域名、证书、systemd 单元或发布身份已经部署成功。生产身份和运行证据必须在后续部署验收中独立确认。
- 失败条件：临时目录、明文密钥、绕过守护进程、无 TLS/反向代理边界或未登记的外部命令均阻断试点发布。
