# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前已推送基线：远端 `main@3f1b69b5ab315442591f40295b036cd0d072be4e`；本地正在准备 R1/Q1/A5 重验候选。
- 最新 SSOT 状态：A4 已以 `EV-A4-ACCEPTED-3` 在离线、来源固定边界内正式接受。
- 当前正式状态：CHARTER、F1、D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2、A4 保持 `ACCEPTED`；R1、Q1、A5 因历史接受身份失效而 `INVALIDATED`，正按 `R1 → Q1 → A5` 严格串行重验。
- 运行代码：L1 `SourceObservation` 合同、L2 `LiteratureBase`、S1 问题状态模型、S2 新颖性审计和 T1 一次性主题观测均已实现；T1 的游标重放、跨批去重、预算/高风险/资料冲突人工通道通过专项与全库回归，外部文献服务仍未启用。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、已接受的决定记录和选择性归档声明。
5. 已完成选择性 Obsidian 快照、快照核对和全集合归档审计；归档集合包含 38 个 bundle 和 23 个已声明问题文档。
6. 已完成 S1 的版本化问题状态模型、三例真实档案纸面 dry-run 和两轮 AI 复审；证据记录见 `evidence/S1.json`。
7. 已完成 T1 的单主题游标持久化、重启重放、跨批去重和人工复核队列；两项资料冲突路径在独立 AI 复审中发现并修复，证据记录见 `evidence/T1.json`。
8. 已完成 T2 的三例来源固定档案演练；固定字节、S1 夹具、预算快照、持久人工队列和重放结果均绑定并通过独立 AI 复审，证据记录见 `evidence/T2.json`。
9. A4 的离线、来源固定工程测试、独立复审和正式记录已闭合；当前证据为 `EV-A4-ACCEPTED-3`。
10. R1 的历史独立复核报告仍可复用为审计材料，但其接受状态随 A4 身份失效而失效。
11. Q1 和 A5 的历史受限记录同样等待重新绑定，不扩大 A4/R1 的科学或发布边界。

## 完成状态

1. A4 已闭合；当前唯一可执行前沿是绑定 `EV-A4-ACCEPTED-3` 的 R1 新候选。
2. R1、Q1、A5 的历史记录均不可作为当前 HEAD 的交付证明；必须按 `R1 → Q1 → A5` 重新冻结、复审和接受。
3. 未校准预测、未完成的新颖性审计和未评估的数学结论继续保留为禁止公开研究结论的边界；本次状态投影已在候选工作树落盘，尚未提交或推送。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 正式计划节点 | 部分需重验 | 14 个节点保持 `ACCEPTED`；R1/Q1/A5 为 `INVALIDATED` |
| GitHub 源级交付 | 已推送当前基线 | `main@3f1b69b5ab315442591f40295b036cd0d072be4e` 与远端一致；本地重验候选尚未交付 |
| 仓库完整本地 CI | 候选待重跑 | 旧基线的 `make ci-full PYTHON=.venv/bin/python` 通过不能外推到当前未提交字节；当前候选完成哈希闭合后将重跑并记录实际计数 |
| 外部文献服务 | 未启用且非本切片阻塞 | L2 仅验证本地字节导入；不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：当前已推送 `main@3f1b69b5` 的复杂度、生成视图、机器源、结构、中文可读性和项目级 Obsidian 快照检查；本地候选完成后将重新运行同一组门禁。

统一验证入口曾用 Python 3.13 严格来源检查通过；当前 local CI 也已通过；这些工程证据不等于数学证明、外部文献确认或生产证据。

统一验证报告保存在 `.ssot/validation-report.json`，会在本次投影更新后重新绑定 manifest。该报告只证明计划工件一致性；即使仓库回归命令通过或 GitHub 引用回读一致，也不能把本计划标记为数学证明、外部文献确认、生产或设备证据、部署完成或公开研究结论。
