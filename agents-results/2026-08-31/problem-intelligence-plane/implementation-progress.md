# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前实现基线提交：远端 `main` 提交 `9d727862a5566b32fdeec3af017dc466a5f9dd12`；A5 验收记录待随本次提交交付。
- 最新 SSOT 元数据提交：远端 `main` 提交 `9d727862a5566b32fdeec3af017dc466a5f9dd12`；A5 状态转换待随本次提交交付。
- 当前正式停止点：D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2、A4、R1、Q1、A5 均已接受；没有待执行的正式节点。
- 运行代码：L1 `SourceObservation` 合同、L2 `LiteratureBase`、S1 问题状态模型、S2 新颖性审计和 T1 一次性主题观测均已实现；T1 的游标重放、跨批去重、预算/高风险/资料冲突人工通道通过专项与全库回归，外部文献服务仍未启用。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、已接受的决定记录和选择性归档声明。
5. 已完成选择性 Obsidian 快照并通过快照核对；全集合归档审计受无关历史 iCloud 占位符清单阻塞，未将该阻塞错误归入本切片。
6. 已完成 S1 的版本化问题状态模型、三例真实档案纸面 dry-run 和两轮 AI 复审；证据记录见 `evidence/S1.json`。
7. 已完成 T1 的单主题游标持久化、重启重放、跨批去重和人工复核队列；两项资料冲突路径在独立 AI 复审中发现并修复，证据记录见 `evidence/T1.json`。
8. 已完成 T2 的三例来源固定档案演练；固定字节、S1 夹具、预算快照、持久人工队列和重放结果均绑定并通过独立 AI 复审，证据记录见 `evidence/T2.json`。
9. 已完成 A4 的独立验收；三例档案、重放、恢复、去重、预算、人工队列及状态篡改失败模式均通过专项、v02、全库回归和独立 AI 复核，证据记录见 `evidence/A4.json`。
10. 已完成 R1 的独立验收；固定三例档案上的四路检索记录、全路/路线增量/留一路损失、身份闭合和保守小样本边界均通过专项、v02 与全库回归，证据记录见 `evidence/R1.json`。两条 AI 复审未返回，不计为通过证据。
11. 已完成 Q1 的独立验收；固定 R1 三例的困难度记录、`UNCALIBRATED` 标记、科学优先级与传播准备度双轨、来源摘要闭合和禁止公开边界均通过专项、v02、全库回归与两条独立 AI 边界复审，证据记录见 `evidence/Q1.json`。
12. 已完成 A5 的范围受限仓库源级发布决定；仅交付已验收仓库源、测试、SSOT 记录和验收证据，保留 Q1 的 `UNCALIBRATED`、`NOT_READY` 与禁止公开研究结论边界，证据记录见 `evidence/A5.json`。

## 下一步

1. 运行 A5 最终专项、全库、SSOT 与快照核验，提交后完成 GitHub 远端引用回读。
2. 保留 A5 的范围边界：未校准预测、未完成的新颖性审计和未评估的数学结论均不得改写为公开发布授权。
3. 仓库回归命令只能作为基线检查；GitHub 引用一致性仅证明仓库源交付，不证明部署或研究结论。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 人工决定记录 | 已接受 | D1、D2、A1 的决定版本为 2；后续节点仍受各自依赖约束 |
| 专项测试文件 | L1、L2 已创建并通过；A2 证据已接受 | 后续切片仍需各自专项测试，不能由全库回归替代 |
| Harness 统一入口 | 执行阻塞 | 统一验证脚本无法完成运行时技能来源检查，不能绑定绿色验证报告 |
| 仓库完整单元测试 | 通过 | `python3 -m unittest discover -s tests` 最近运行 400 项，400 项通过，10 项跳过；独立仓库根级 `registry.yaml` 已纳入 Gate 0 |
| 外部文献服务 | 尚未启用 | L2 仅验证本地字节导入；不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：生成视图检查、机器源检查（刷新模式）、结构检查（刷新模式）、中文可读性检查、选择性 Obsidian 快照核对和全集合归档审计。

基线回归：`make ci-full`、`make clean-ci` 和 `make baseline` 均已通过；Gate 0 registry 迁移缺口与根仓库归档边界已修复。统一验证已绑定上一验证提交；本次 L2 提交后的 SSOT 验证报告需重新生成。

统一验证报告保存在 `.ssot/validation-report.json`，并由 `.ssot/manifest.json` 绑定。该报告只证明计划工件一致性；D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2、A4、R1、Q1、A5 已接受。即使仓库回归命令通过或 GitHub 引用回读一致，也不能把本计划标记为部署完成、生产验收或数学结论。
