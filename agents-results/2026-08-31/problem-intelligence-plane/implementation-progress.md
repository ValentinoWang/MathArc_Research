# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前项目提交：`05626d82275419cd9b3cb9f1f3648f2b3d1bfb2e`。
- 当前正式停止点：D1、D2、A1 已接受；L1、L2 实现证据和 A2 机器验收准备已完成，仍等待指定负责人正式验收。
- 运行代码：L1 `SourceObservation` 合同和 L2 `LiteratureBase` 已实现并通过专项测试；L2 的正式验收节点 A2 仍阻塞后续状态模型。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、待拍板文件和选择性归档声明。
5. 已完成选择性 Obsidian 快照，并通过快照核对和全集合归档审计。

## 下一步

1. 由研究基础设施负责人正式验收 L1/L2，并补写 A2 evidence-only 记录。
2. A2 正式接受后，按依赖前沿推进 S1；S1 的验收必须先对 T2 三例真实档案做纸面 dry-run。
3. 继续实现 S2、A3、T1、T2、A4、R1、Q1、A5；不得把实现证据提前写成正式验收。
4. 每个后续验收切片补建真实专项测试和独立 evidence 文件；仓库回归命令只能作为基线检查。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 人工决定记录 | 已接受 | D1、D2、A1 的决定版本为 2；后续节点仍受各自依赖约束 |
| 专项测试文件 | L1、L2 已创建并通过；A2 证据已准备 | 后续切片仍需各自专项测试，不能由全库回归替代 |
| Harness 统一入口 | 执行阻塞 | 统一验证脚本无法完成运行时技能来源检查，不能绑定绿色验证报告 |
| 仓库完整单元测试 | 通过 | `python3 -m unittest discover -s tests` 共运行 302 项，302 项通过，10 项跳过；独立仓库根级 `registry.yaml` 已纳入 Gate 0 |
| 外部文献服务 | 尚未启用 | L2 仅验证本地字节导入；不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：生成视图检查、机器源检查（刷新模式）、结构检查（刷新模式）、中文可读性检查、选择性 Obsidian 快照核对和全集合归档审计。

基线回归：`make ci-full`、`make clean-ci` 和 `make baseline` 均已通过；Gate 0 registry 迁移缺口与根仓库归档边界已修复。统一验证已绑定上一验证提交；本次 L2 提交后的 SSOT 验证报告需重新生成。

统一验证报告保存在 `.ssot/validation-report.json`，并已由 `.ssot/manifest.json` 绑定。报告中的 Gate 0、运行时来源、视图、机器程序、结构、中文可读性与归档检查均通过；这只证明计划工件一致性，不改变 L1 尚未完成正式人工验收、L2 及后续节点仍未完成的结论。即使仓库回归命令通过，也不能把本计划标记为部署完成或生产验收。
