# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前已推送基线：远端 `main@329b270460abf146561499fd5aa7ec4e62737eb1`；当前工作树含待提交的 R1 重新打开修订。
- 最新 SSOT 状态：A4 已在 `EV-A4-ACCEPTED-2` 的离线、来源固定边界内接受；该边界不构成数学证明、外部文献确认、生产或设备证据，也不构成公开研究授权。
- 当前正式停止点：CHARTER、F1、D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2、A4 已 `ACCEPTED`；R1 已重新打开但等待两份新的独立、不同包装器持久化通过报告，Q1、A5 因 R1 未重新接受而 `BLOCKED`。
- 运行代码：L1 `SourceObservation` 合同、L2 `LiteratureBase`、S1 问题状态模型、S2 新颖性审计和 T1 一次性主题观测均已实现；T1 的游标重放、跨批去重、预算/高风险/资料冲突人工通道通过专项与全库回归，外部文献服务仍未启用。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、已接受的决定记录和选择性归档声明。
5. 已完成选择性 Obsidian 快照、快照核对和全集合归档审计；归档集合包含 38 个 bundle 和 23 个已声明问题文档。
6. 已完成 S1 的版本化问题状态模型、三例真实档案纸面 dry-run 和两轮 AI 复审；证据记录见 `evidence/S1.json`。
7. 已完成 T1 的单主题游标持久化、重启重放、跨批去重和人工复核队列；两项资料冲突路径在独立 AI 复审中发现并修复，证据记录见 `evidence/T1.json`。
8. 已完成 T2 的三例来源固定档案演练；固定字节、S1 夹具、预算快照、持久人工队列和重放结果均绑定并通过独立 AI 复审，证据记录见 `evidence/T2.json`。
9. A4 已在 `EV-A4-ACCEPTED-2` 的离线、来源固定验收边界内接受；该记录不延伸为数学证明、外部文献确认、生产或设备证据，亦不构成公开研究授权。
10. R1 已基于 A4 新身份重新打开，但须取得两份新的、相互独立且使用不同包装器的持久化通过报告后方可重新接受；历史复核记录只作审计材料。
11. Q1 和 A5 均依赖 R1 的新接受结果，当前保持阻塞；历史接受声明不代表当前状态。

## 完成状态

1. A4 的正式接受记录为 `EV-A4-ACCEPTED-2`，但其范围限于离线固定来源验收；R1、Q1、A5 的历史材料不解锁当前依赖链。
2. 当前正式验收链在重新打开的 R1 停止，等待两份新的独立、不同包装器持久化通过报告；Q1 和 A5 必须等待 R1 新接受。
3. 未校准预测、未完成的新颖性审计和未评估的数学结论继续保留为禁止公开研究结论的边界；当前工作树的 R1 重新打开修订尚未形成新的远端交付。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 正式计划节点 | 部分完成 | 14/17 节点为 `ACCEPTED`；R1 已重新打开但等待两份新的独立、不同包装器持久化通过报告，Q1/A5 为 `BLOCKED` |
| GitHub 源级交付 | 已推送基线可读回 | `main@329b270...` 为已推送基线；当前 R1 重新打开修订仍在工作树，尚无新的远端交付声明 |
| 仓库完整单元测试 | 通过 | `python3.13 -m unittest discover -s tests` 最近运行 509 项，509 项通过，10 项跳过；独立仓库根级 `registry.yaml` 已纳入 Gate 0 |
| 外部文献服务 | 未启用且非本切片阻塞 | L2 仅验证本地字节导入；不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：已推送基线的复杂度、生成视图、机器源、结构、中文可读性和选择性 Obsidian 快照检查。当前 R1 重新打开投影尚未重新生成视图或刷新对应绑定。

已推送基线的统一验证入口曾用 Python 3.13 严格来源检查通过。当前 R1 重新打开投影必须在机器源同步后重新渲染和验证；`make ci-full` 等仓库基线证据不等于 A4 或 R1 的正式验收。

统一验证报告保存在 `.ssot/validation-report.json`，其现有 manifest 绑定只覆盖已推送基线，不覆盖当前 R1 重新打开投影。该报告只证明计划工件一致性；当前正式停止点为 R1。即使仓库回归命令通过或 GitHub 引用回读一致，也不能把本计划标记为数学证明、外部文献确认、生产或设备证据、部署完成或公开研究结论。
