# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前实现基线提交：远端 `main` 提交 `0cbf6a843cd0a5ff065e684e54e501b031b251ef`；R1 因两条独立 AI 复审缺少持久化报告而重新打开。
- 最新 SSOT 元数据提交：远端 `main` 提交 `0cbf6a843cd0a5ff065e684e54e501b031b251ef`；本次修订只失效 R1 及其后继 Q1/A5，历史证据不再作为通过依据。
- 当前正式停止点：D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2、A4 已接受；R1 为 `READY`，Q1/A5 因上游 R1 重新验收而 `BLOCKED`。
- 运行代码：L1 `SourceObservation` 合同、L2 `LiteratureBase`、S1 问题状态模型、S2 新颖性审计和 T1 一次性主题观测均已实现；T1 的游标重放、跨批去重、预算/高风险/资料冲突人工通道通过专项与全库回归，外部文献服务仍未启用。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、已接受的决定记录和选择性归档声明。
5. 已完成选择性 Obsidian 快照、快照核对和全集合归档审计；归档集合包含 36 个 bundle 和 24 个已声明问题文档。
6. 已完成 S1 的版本化问题状态模型、三例真实档案纸面 dry-run 和两轮 AI 复审；证据记录见 `evidence/S1.json`。
7. 已完成 T1 的单主题游标持久化、重启重放、跨批去重和人工复核队列；两项资料冲突路径在独立 AI 复审中发现并修复，证据记录见 `evidence/T1.json`。
8. 已完成 T2 的三例来源固定档案演练；固定字节、S1 夹具、预算快照、持久人工队列和重放结果均绑定并通过独立 AI 复审，证据记录见 `evidence/T2.json`。
9. 已完成 A4 的独立验收；三例档案、重放、恢复、去重、预算、人工队列及状态篡改失败模式均通过专项、v02、全库回归和独立 AI 复核，证据记录见 `evidence/A4.json`。
10. R1 已重新打开：旧记录的两条 AI 复审未返回持久化报告，不再以测试替代独立复审；合同版本 4 要求两份不同审阅身份、不同包装器、同一冻结输入的零写入 `PASS` 报告。
11. Q1 和 A5 的旧接受记录被保留为历史审计材料，但因分别固定消费 R1/Q1 身份，必须在 R1 重新接受后按依赖顺序重新绑定和验收。

## 完成状态

1. A5 的历史专项、全库、SSOT、验收索引、Obsidian 快照与全集合归档审计不再证明当前 R1/Q1/A5 接受链。
2. R1 的合同硬化已进入正式前沿；Q1 和 A5 必须待 R1 的两份独立报告和重新验收完成后再分别运行。
3. 未校准预测、未完成的新颖性审计和未评估的数学结论继续保留为禁止公开研究结论的边界；此前 GitHub 引用一致性只证明当时的仓库源交付。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 正式计划节点 | 重新验收中 | 14/17 节点为 `ACCEPTED`；R1 为 `READY`，Q1/A5 为依赖阻断 |
| GitHub 源级交付 | 历史交付 | 旧 A5 提交仍在远端，但不证明重新打开后的 R1/Q1/A5 接受链 |
| 仓库完整单元测试 | 通过 | `python3.13 -m unittest discover -s tests` 最近运行 404 项，404 项通过，10 项跳过；独立仓库根级 `registry.yaml` 已纳入 Gate 0 |
| 外部文献服务 | 未启用且非本切片阻塞 | L2 仅验证本地字节导入；不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：生成视图检查、机器源检查（刷新模式）、结构检查（刷新模式）、中文可读性检查、选择性 Obsidian 快照核对和全集合归档审计。

基线回归：`make ci-full`、`make clean-ci` 和 `make baseline` 均已通过；Gate 0 registry 迁移缺口与根仓库归档边界已修复。A5 最终复验已运行专项与全库单元测试，严格 SSOT 验证报告已刷新并绑定到 manifest。

统一验证报告保存在 `.ssot/validation-report.json`，并由 `.ssot/manifest.json` 绑定。该报告只证明计划工件一致性；D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2、A4 已接受，R1/Q1/A5 正在重新建立可接受证据。即使仓库回归命令通过或 GitHub 引用回读一致，也不能把本计划标记为部署完成、生产验收或数学结论。
