# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前实现基线提交：远端 `main` 提交 `fe9de3fd86e3670dc3a0c10621afa48fc4740fa8`；当前工作树仍有未提交修改。
- 最新 SSOT 元数据：当前工作树内的 A4 状态回退与证据边界修订尚未形成远端提交；不得沿用历史 A5 交付声明。
- 当前正式停止点：CHARTER、F1、D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2 已 `ACCEPTED`；A4 为 `READY`，R1、Q1、A5 为 `BLOCKED`。
- 运行代码：L1 `SourceObservation` 合同、L2 `LiteratureBase`、S1 问题状态模型、S2 新颖性审计和 T1 一次性主题观测均已实现；T1 的游标重放、跨批去重、预算/高风险/资料冲突人工通道通过专项与全库回归，外部文献服务仍未启用。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、已接受的决定记录和选择性归档声明。
5. 已完成选择性 Obsidian 快照、快照核对和全集合归档审计；归档集合包含 36 个 bundle 和 24 个已声明问题文档。
6. 已完成 S1 的版本化问题状态模型、三例真实档案纸面 dry-run 和两轮 AI 复审；证据记录见 `evidence/S1.json`。
7. 已完成 T1 的单主题游标持久化、重启重放、跨批去重和人工复核队列；两项资料冲突路径在独立 AI 复审中发现并修复，证据记录见 `evidence/T1.json`。
8. 已完成 T2 的三例来源固定档案演练；固定字节、S1 夹具、预算快照、持久人工队列和重放结果均绑定并通过独立 AI 复审，证据记录见 `evidence/T2.json`。
9. 已完成 A4 的专项与独立复核，但正式 A4 验收结果尚未执行；证据记录见 `evidence/A4.json`。
10. R1 因缺少 A4 正式验收而保持阻塞；历史复核记录仅作审计材料。
11. Q1 和 A5 依赖 R1，当前均保持阻塞；历史接受声明不代表当前状态。

## 完成状态

1. A4 的发布运行记录（release run）仅包含独立复核返回，缺少正式验收结果；R1、Q1、A5 的历史材料不解锁当前依赖链。
2. 当前正式验收链在 A4 停止；不得把历史 R1/Q1/A5 接受声明投影为当前节点状态。
3. 未校准预测、未完成的新颖性审计和未评估的数学结论继续保留为禁止公开研究结论的边界；当前没有新的 SSOT 远端交付声明。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 正式计划节点 | 部分完成 | 13/17 节点为 `ACCEPTED`；A4 为 `READY`，R1/Q1/A5 为 `BLOCKED` |
| GitHub 源级交付 | 仅基线可读回 | `main` 的 fe9de3f... 与远端一致；当前 SSOT 修订仍在工作树 |
| 仓库完整单元测试 | 通过 | `python3.13 -m unittest discover -s tests` 最近运行 494 项，494 项通过，2 项跳过；独立仓库根级 `registry.yaml` 已纳入 Gate 0 |
| 外部文献服务 | 未启用且非本切片阻塞 | L2 仅验证本地字节导入；不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：复杂度检查、生成视图检查和中文可读性检查；机器源/结构的直接复核受验证报告失败绑定约束，选择性 Obsidian 快照检查也因受管文件哈希漂移失败。

统一验证入口已用 Python 3.13 重跑：运行时来源、复杂度、生成视图、机器源、结构和中文可读性检查在报告生成阶段通过；归档检查因 Obsidian 快照受管文件哈希与当前源不一致而失败，因此报告结果为 `fail`，机器源绑定不能宣称通过。`make ci-full` 等仓库基线证据不等于 A4 正式验收。

统一验证报告保存在 `.ssot/validation-report.json`；因归档检查失败，本次报告结果为 `fail`，未写入新的 manifest 绑定。该报告只证明计划工件一致性；当前正式停止点为 A4 `READY`。即使仓库回归命令通过或 GitHub 引用回读一致，也不能把本计划标记为部署完成、生产验收或数学结论。
