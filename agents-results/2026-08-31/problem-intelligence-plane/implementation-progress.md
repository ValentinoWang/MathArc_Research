# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前项目提交：`797b20fe91776602037ad22ae667086099def189`。
- 当前正式停止点：D1、D2、A1 已接受；当前实现停止点为 L1 合同编译。
- 运行代码：本轮没有修改，五个发布切片均未进入实现验收。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、待拍板文件和选择性归档声明。
5. 已完成选择性 Obsidian 快照，并通过快照核对和全集合归档审计。

## 下一步

1. 完成 L1 资料观察合同并锁定其验收片段。
2. 并行准备 L2、S1、S2、T1、T2、R1 的实现与测试，但只在上游验收节点接受后合并。
3. 依赖满足后进入开放资料导入和专项测试。
4. 为每个后续验收切片补建真实的专项测试文件；当前仓库回归命令只能作为基线检查，不能单独证明新能力已实现。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 人工决定记录 | 已接受 | D1、D2、A1 的决定版本为 2；后续节点仍受各自依赖约束 |
| 专项测试文件 | 尚未创建 | 当前只能运行仓库既有回归，不能证明问题情报行为 |
| Harness 统一入口 | 执行阻塞 | 统一验证脚本无法完成运行时技能来源检查，不能绑定绿色验证报告 |
| 仓库完整单元测试 | 通过 | `python3 -m unittest discover -s tests` 共运行 293 项，293 项通过，10 项跳过；独立仓库根级 `registry.yaml` 已纳入 Gate 0 |
| 外部文献服务 | 尚未启用 | 不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：生成视图检查、机器源检查（刷新模式）、结构检查（刷新模式）、中文可读性检查、选择性 Obsidian 快照核对和全集合归档审计。

基线回归：`python3 -m unittest discover -s tests` 已通过；Gate 0 registry 迁移缺口已修复。统一验证仍受 `.harness/manifest.yaml` 缺失阻塞。

统一验证报告保存在 `.ssot/validation-report.json`。已使用本机具备 `PyYAML` 的 Python 3.13 重跑统一入口，运行技能来源检查明确停在项目缺少 `.harness/manifest.yaml`，且当前没有 `.harness/lock.yaml`。因此严格 schema-v2 的绿色验证报告尚未绑定到 `manifest.json`；这只阻塞计划验证闭环，不改变人工决定和功能实现均未完成的结论。即使仓库回归命令通过，也不能把本计划标记为功能实现、部署完成或生产验收。
