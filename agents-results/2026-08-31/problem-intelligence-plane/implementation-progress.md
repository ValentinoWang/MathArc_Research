# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前已推送基线：远端 `main@fed4ab595aba28edd6b811a8400950c023dab1cb`；当前工作树干净。
- 最新 SSOT 状态：A4 已在 `EV-A4-ACCEPTED-2` 的离线、来源固定边界内接受；该边界不构成数学证明、外部文献确认、生产或设备证据，也不构成公开研究授权。
- 当前正式状态：CHARTER、F1、D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2、A4、R1、Q1、A5 均为 `ACCEPTED`，但接受边界仍严格限于各自合同。
- 运行代码：L1 `SourceObservation` 合同、L2 `LiteratureBase`、S1 问题状态模型、S2 新颖性审计和 T1 一次性主题观测均已实现；T1 的游标重放、跨批去重、预算/高风险/资料冲突人工通道通过专项与全库回归，外部文献服务仍未启用。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、已接受的决定记录和选择性归档声明。
5. 已完成选择性 Obsidian 快照、快照核对和全集合归档审计；归档集合包含 38 个 bundle 和 23 个已声明问题文档。
6. 已完成 S1 的版本化问题状态模型、三例真实档案纸面 dry-run 和两轮 AI 复审；证据记录见 `evidence/S1.json`。
7. 已完成 T1 的单主题游标持久化、重启重放、跨批去重和人工复核队列；两项资料冲突路径在独立 AI 复审中发现并修复，证据记录见 `evidence/T1.json`。
8. 已完成 T2 的三例来源固定档案演练；固定字节、S1 夹具、预算快照、持久人工队列和重放结果均绑定并通过独立 AI 复审，证据记录见 `evidence/T2.json`。
9. A4 已在 `EV-A4-ACCEPTED-2` 的离线、来源固定验收边界内接受；该记录不延伸为数学证明、外部文献确认、生产或设备证据，亦不构成公开研究授权。
10. R1 已基于 A4 新身份重新绑定，并取得两份相互独立且使用不同包装器的持久化通过报告后接受；历史复核记录只作审计材料。
11. Q1 和 A5 已分别完成受限验收；其接受不扩大 A4/R1 的科学或发布边界。

## 完成状态

1. A4 的正式接受记录为 `EV-A4-ACCEPTED-2`，范围限于离线固定来源验收；R1、Q1、A5 也已在各自合同范围内完成接受。
2. 当前正式验收链已闭合至 A5；R1、Q1、A5 的接受仍不延伸为数学证明、外部文献确认、生产或公开发布授权。
3. 未校准预测、未完成的新颖性审计和未评估的数学结论继续保留为禁止公开研究结论的边界；本次状态投影已落盘并推送到远端 `main`。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 正式计划节点 | 已完成本轮验收 | 17/17 节点为 `ACCEPTED`，接受边界仍按各节点合同分别成立 |
| GitHub 源级交付 | 已推送并可读回 | `main@fed4ab595aba28edd6b811a8400950c023dab1cb` 与远端一致 |
| 仓库完整单元测试 | 通过 | `make ci PYTHON=.venv/bin/python` 当前运行 511 项，511 项通过，2 项跳过；20 项 SMT 全部执行；独立仓库根级 `registry.yaml` 已纳入 Gate 0；M3/M4 工作区隔离负测通过 |
| 外部文献服务 | 未启用且非本切片阻塞 | L2 仅验证本地字节导入；不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：当前 `main@fed4ab5` 的复杂度、生成视图、机器源、结构、中文可读性和选择性 Obsidian 快照检查。

统一验证入口曾用 Python 3.13 严格来源检查通过；当前 local CI 也已通过；这些工程证据不等于数学证明、外部文献确认或生产证据。

统一验证报告保存在 `.ssot/validation-report.json`，会在本次投影更新后重新绑定 manifest。该报告只证明计划工件一致性；即使仓库回归命令通过或 GitHub 引用回读一致，也不能把本计划标记为数学证明、外部文献确认、生产或设备证据、部署完成或公开研究结论。
