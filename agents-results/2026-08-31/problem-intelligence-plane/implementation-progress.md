# 问题情报平面实施进度

本文件是 PLAN_VERSION 3 / DAG_VERSION 2 的运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json`、节点/边分片及本次计划源文件为准。本次工作仅更新 SSOT 计划，不实现代码、不进行验收、不发布服务。

## 当前状态

- 计划深度：L2 Release SSOT；当前计划包含 7 个发布切片、25 个节点和 W0-W22 波次。
- 既有旧状态：A4、R1、Q1、A5 均为 `INVALIDATED`；它们的历史接受记录只作审计材料。
- 新增决定：`decision.problem-intelligence.console-slices@1` 的用户决定内容已由研究负责人和仓库所有者批准；对应执行节点 D3 当前 `BLOCKED`，不解锁 U1。
- 新增节点：D3、U1、U2、U3、A6、U4、U5、A7 均未完成实现或验收。A6/A7 执行合同和人工 H-01 清单均为 `DRAFT`/`PENDING`。
- 控制台范围：PI-R6 为控制台接线，PI-R7 为本地域投影与运营域；landing/login 归属仍待产品决定，但不是当前 D3 阻塞范围。

## 计划更新

1. 将计划头收敛为 `PLAN_VERSION: 3`、`DAG_VERSION: 2`，保留 `INTERFACE_FREEZE_VERSION: 2` 和 `NODE_CONTRACT_VERSION: 2`。
2. 将原有 PI-R1 至 PI-R5 与新增 PI-R6、PI-R7 统一记录，新增 8 个节点、W16-W22 和 14 条真实硬边。
3. 将控制台来源要求绑定到 `source-requirements.json` 及已追加的控制台视图合同；该绑定是源级计划材料，不是实现或验收证据。
4. 明确 D3 的“决定已批准”和“执行节点阻塞”是两个不同状态；不把现有原型、静态导出、测试或视觉材料升级为 A6/A7 证据。
5. 新增严格 UI change 声明、三栏视觉工作台、32 个唯一视图与 52 个渲染/状态案例合同，并把七份新增验收合同绑定到同一来源身份；所有合同仍保持 `DRAFT`/`PLANNED`。
6. 第六发布切片的开发冻结与晋升重放角色分别登记，但都解析到用户批准的底层身份 `accepted:pi-r5-v1`；这只满足三基线角色可区分要求，不制造第二个源码基线。

## 验证边界

- 正式兼容基准固定为 Harness `928556ced521716e82ddc331b0e83921bb67bb3c`；当前 Harness 主工作树只用于前向诊断，其未提交改动和新增规则不作为本计划的正式通过依据。
- 旧的 A4、R1、Q1、A5 合同不因 PLAN v3 被静默改写；若当前 Harness 新 schema 要求给这些已批准合同新增字段，必须另行修订和重验。
- 目标 Obsidian 快照只复制声明的主文档和 `openproblem.md`；项目内机器源、进度、合同、工作台和证据不进入快照，也不因此成为第二套 SSOT。

## 阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| A4/R1/Q1/A5 | `INVALIDATED` | 旧链路不得以历史记录恢复为当前验收或发布状态 |
| D3 | `BLOCKED` | 用户决定内容已批准，但 A5 未恢复前不能解锁 U1 |
| U1/U2/U3 | `BLOCKED` | 不得声称视觉基线、只读接线或评审闭环已实现 |
| A6 | `BLOCKED` | 合同与人工 H-01 清单为 `DRAFT`/`PENDING`，无 acceptance |
| U4/U5 | `BLOCKED` | 等待 A6 及其声明的旧节点输入，不声称投影或运营账本实现 |
| A7 | `BLOCKED` | 合同与人工 H-01 清单为 `DRAFT`/`PENDING`，不作 v0 发布决定 |

## 下一步资格

当前唯一允许的动作是等待 A4 失效链路按既有规则修复并重验；在 A5 恢复前不得启动 D3 执行链。A5 恢复后仍须按 W16-W22 顺序执行 D3、U1、U2、U3、A6、U4/U5 和 A7，并分别取得机器证据与人工签字。

本投影不声称任何运行代码、控制台功能、人工 acceptance、生产部署、设备结果、外部文献确认、数学证明或公开研究结论。
