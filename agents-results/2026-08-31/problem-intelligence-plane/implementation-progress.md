# 问题情报平面实施进度

本文件是运行投影，不是编排权威。编排、依赖、决定版本和失效范围以同目录 `.ssot/manifest.json` 及其分片为准。

## 当前状态

- 计划深度：L2 Release SSOT。
- 计划日期：2026-08-31。
- 当前已推送基线：远端 `main@5e06216edbb36e42b5b5c6686e6792c84959f9da`；本地正在落盘 A4 失效状态。
- 最新 SSOT 状态：A4 的历史 `EV-A4-ACCEPTED-3` 已转为审计历史；双独立 AI 复核确认四项实现缺陷，当前证据为 `EV-A4-REOPENED-4`。
- 当前正式状态：CHARTER、F1、D1、D2、A1、L1、L2、A2、S1、S2、A3、T1、T2 保持 `ACCEPTED`；A4、R1、Q1、A5 均为 `INVALIDATED`。A4 修复和重验完成前，下游不得重启。
- 运行代码：L1 `SourceObservation` 合同、L2 `LiteratureBase`、S1 问题状态模型、S2 新颖性审计和 T1 一次性主题观测均已实现；T1 的游标重放、跨批去重、预算/高风险/资料冲突人工通道通过专项与全库回归，外部文献服务仍未启用。

## 已完成

1. 已把评审建议编译为五个独立发布切片、17 个节点和显式依赖边。
2. 已记录治理准入、事实对账、活动假设和权威冲突。
3. 已把当前权威文件的提交身份和内容校验值登记到机器源。
4. 已补齐主视图、已接受的决定记录和选择性归档声明。
5. 已完成选择性 Obsidian 快照、快照核对和全集合归档审计；归档集合包含 38 个 bundle 和 23 个已声明问题文档。
6. 已完成 S1 的版本化问题状态模型、三例真实档案纸面 dry-run 和两轮 AI 复审；证据记录见 `evidence/S1.json`。
7. 已完成 T1 的单主题游标持久化、重启重放、跨批去重和人工复核队列；两项资料冲突路径在独立 AI 复审中发现并修复，证据记录见 `evidence/T1.json`。
8. 已完成 T2 的三例来源固定档案演练；固定字节、S1 夹具、预算快照、持久人工队列和重放结果均绑定并通过独立 AI 复审，证据记录见 `evidence/T2.json`。
9. A4 的历史离线、来源固定验收记录已保存在 `evidence/history/A4-accepted-3.json`，不再代表当前实现。
10. `reviews-d77e5c1` 两条独立零写入复核均合法返回 `FAIL`，共同确认根目录耐久性、预算升级、已处理重放和 PENDING 不可变性四项缺陷。
11. R1、Q1 和 A5 的历史记录只作审计材料；A4 重新接受前不得重绑或代签。

## 完成状态

1. 当前唯一可执行前沿是 A4 的四项实现修复；修复完成后才生成内容摘要绑定的 v3 候选。
2. R1 v11 保持字节不变并继续 `INVALIDATED`；Q1、A5 的人工决定继续失效，不能由 AI 代签。
3. 未校准预测、未完成的新颖性审计和未评估的数学结论继续保留为禁止公开研究结论的边界。

## 当前阻塞与证据边界

| 项目 | 状态 | 影响 |
|---|---|---|
| 正式计划节点 | A4 失效并阻塞 | 13 个节点保持 `ACCEPTED`；A4/R1/Q1/A5 为 `INVALIDATED` |
| GitHub 源级交付 | 双 FAIL campaign 已推送 | `main@5e06216edbb36e42b5b5c6686e6792c84959f9da` 与远端一致；状态修订待本阶段推送 |
| 仓库完整本地 CI | 旧候选通过但不足以验收 | `d77e5c1` 记录 567/567、SMT 20/20、零错误；独立复核证明这些测试未覆盖四项缺陷 |
| 外部文献服务 | 未启用且非本切片阻塞 | L2 仅验证本地字节导入；不产生任何外部抓取或生产证据 |

## 验证记录

已通过：复杂度检查（L2、5 个发布切片、17 个节点、0 个 worker）。

已通过：锁定 Harness `928556ced521716e82ddc331b0e83921bb67bb3c` 的统一 SSOT 验证、复杂度、生成视图、机器源、结构、中文可读性和项目级 Obsidian 快照检查。全局归档审计仍被 Harness 工作树中并发修改的 `harness-delivery-console-v2` 快照漂移阻塞，最终收尾必须重试；本仓库不得修改该外部 bundle。

统一验证入口曾用 Python 3.13 严格来源检查通过；当前 local CI 也已通过；这些工程证据不等于数学证明、外部文献确认或生产证据。

统一验证报告保存在 `.ssot/validation-report.json`，会在本次投影更新后重新绑定 manifest。该报告只证明计划工件一致性；即使仓库回归命令通过或 GitHub 引用回读一致，也不能把本计划标记为数学证明、外部文献确认、生产或设备证据、部署完成或公开研究结论。
